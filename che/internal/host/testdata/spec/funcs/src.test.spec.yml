# [>] 🤖🤖
context:
  function: host.Host.Src
cases:
  - name: ShouldJoinRelUnderRepoRootTree
    input:
      args: [repoRoot: /repo, rel: etc/zshrc]
    expected:
      output: /repo/root/etc/zshrc
# [<] 🤖🤖
