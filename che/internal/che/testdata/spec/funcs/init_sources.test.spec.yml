# [>] 🤖🤖
context:
  function: che.InitSources
cases:
  - name: ShouldCloneRemoteRefSpecSources
    input:
      args:
        - refFiles:
            che.yml: |
              profilesDefinitions:
                gitlabGroup:
                  type: host
                  include:
                    runScripts: [scripts/bootstrap.zsh]
            scripts/bootstrap.zsh: "#!/bin/zsh\n"
        - hostFiles:
            che.yml: |
              profilesDefinitions:
                main:
                  type: host
                  options: {autoDiscover: true}
                  include:
                    profiles:
                      - url: "file://${REF_DIR}"
                        specDirPath: .
                        profileName: gitlabGroup
        - flags: {prepareAfter: true}
    expected:
      stdOut:
        - 'detected remote ref profile gitlabGroup: git::file://${REF_DIR} profile gitlabGroup'
        - 'cloned remote file://${REF_DIR}'
  - name: ShouldFetchRefEvenWhenItsConditionFails
    input:
      args:
        - refFiles:
            che.yml: |
              profilesDefinitions:
                gitlabGroup:
                  type: host
                  options:
                    runIf: ['env:REF_GATE']
        - hostFiles:
            che.yml: |
              profilesDefinitions:
                main:
                  type: host
                  options: {autoDiscover: true}
                  include:
                    profiles:
                      - url: "file://${REF_DIR}"
                        specDirPath: .
                        profileName: gitlabGroup
    expected:
      stdOut:
        - 'cloned remote file://${REF_DIR}'
    notExpected:
      stdOut:
        - 'evaluated run if'
  - name: ShouldSkipRemoteRefsWithOptionSet
    input:
      args:
        - refFiles:
            che.yml: |
              profilesDefinitions:
                gitlabGroup: {type: host}
        - hostFiles:
            che.yml: |
              profilesDefinitions:
                main:
                  type: host
                  options: {autoDiscover: true}
                  include:
                    profiles:
                      - url: "file://${REF_DIR}"
                        specDirPath: .
                        profileName: gitlabGroup
        - flags: {skipRemoteRefs: true}
    notExpected:
      stdOut:
        - 'cloned remote'
  - name: ShouldAbortOnUnreachableRemoteWithoutCache
    input:
      args:
        - refFiles:
            che.yml: |
              profilesDefinitions:
                gitlabGroup: {type: host}
        - hostFiles:
            che.yml: |
              profilesDefinitions:
                main:
                  type: host
                  options: {autoDiscover: true}
                  include:
                    profiles:
                      - url: "file:///nonexistent-che-src"
                        specDirPath: .
                        profileName: gitlabGroup
    expected:
      errorOutput: 'init-remote-sources gitlabGroup: '
# [<] 🤖🤖
