# [>] 🤖🤖
context:
  function: fsutil.NormalizeOS
cases:
  - name: normalizeOsShouldMapDarwinToMacos
    input:
      args: [os: darwin]
    expected:
      output: macos
  - name: normalizeOsShouldKeepLinux
    input:
      args: [os: linux]
    expected:
      output: linux
# [<] 🤖🤖
