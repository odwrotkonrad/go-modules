##[>] 🤖🤖
profilesDefinitions:
  base:
    include:
      renderTemplates:
        - source: [a.tpl, b.tpl]
          dest: docs

  'cli/macos':
    options: {autoDiscover: true}
    include:
      profiles: [base]

##[<] 🤖🤖
