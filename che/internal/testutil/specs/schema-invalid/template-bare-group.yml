##[>] 🤖🤖
profilesDefinitions:
  base:
    type: host
    include:
      renderTemplates:
        - options: {skipAutoGeneratedHeader: true}
          <<<:
            - {source: a.ontoRepo.tpl, dest: a.md}

  'cli/macos':
    type: host
    options: {autoDiscover: true}
    include:
      profiles: [base]

##[<] 🤖🤖