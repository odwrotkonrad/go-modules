##[>] 🤖🤖
profilesDefinitions:
  base:
    include:
      renderTemplates:
        - source: [a.tpl, "git::gitlab.com/acme/tools//b.tpl"]

  'cli/macos':
    options: {autoDiscover: true}
    include:
      profiles: [base]

##[<] 🤖🤖
