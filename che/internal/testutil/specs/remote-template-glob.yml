profilesDefinitions:
  base:
    type: host
    include:
      renderTemplates:
        - "git::gitlab.com/acme/tools//snippets/*.md"

  'cli/macos':
    type: host
    options: {autoDiscover: true}
    include:
      profiles: [base]
