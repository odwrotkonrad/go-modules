#[>] 🤖🤖
base:
  include:
    services: [svc]
main:
  include:
    profiles:
      - base
      - ref: p
        options: {source: "@git@gitlab.com:g/r.git"}
      - ref: q
        options: {source: "@https://gitlab.com/g/r2.git"}
        env:
          GITLAB_GROUP: konradodwrot
      - ref: p
        options: {source: "@git@gitlab.com:g/r.git"}
        env:
          IGNORED: dup
#[<] 🤖🤖
