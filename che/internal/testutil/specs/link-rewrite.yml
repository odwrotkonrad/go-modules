##[>] 🤖🤖
base:
  include:
    link:
      - etc/zshrc
      - {source: HOME/.config/foo/**, dest: s/foo/bar/}

cli/macos:
  options: {autoDiscover: true}
  include:
    profiles: [base]

exclude-rewritten:
  include:
    profiles: [base]
  exclude:
    link:
      - HOME/.config/bar/b
##[<] 🤖🤖
