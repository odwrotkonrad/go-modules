##[>] 🤖🤖
base:
  include:
    makeLinks:
      - etc/zshrc
      - {source: _home/.config/foo/**, dest: s/foo/bar/}

cli/macos:
  options: {autoDiscover: true}
  include:
    profiles: [base]

exclude-rewritten:
  include:
    profiles: [base]
  exclude:
    makeLinks:
      - _home/.config/bar/b
##[<] 🤖🤖
