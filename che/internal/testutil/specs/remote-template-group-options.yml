profilesDefinitions:
  base:
    type: host
    include:
      renderTemplates:
        - source: "git::gitlab.com/acme/tools"
          options: {skipAutoGeneratedHeader: true}
          <<<:
            - source: //snippets/ro.md
              dest:
                - {path: .claude/agents/ro.md}
            - source: //snippets/settings.json
              dest:
                - {path: .claude/settings.json, options: {renderReferencedFiles: true}}
            - source: //snippets/gitignore
              dest:
                - {path: .claude/.gitignore, options: {skipAutoGeneratedHeader: false}}

  'cli/macos':
    type: host
    options: {autoDiscover: true}
    include:
      profiles: [base]
