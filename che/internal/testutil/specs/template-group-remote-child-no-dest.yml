##[>] 🤖🤖
profilesDefinitions:
  base:
    type: host
    include:
      renderTemplates:
        - source: "git::gitlab.com/acme/tools@v1.0.0"
          dest: shared/ci/ruby
          <<<:
            - source: //snippets/artifact.rb

  'cli/macos':
    type: host
    options: {autoDiscover: true}
    include:
      profiles: [base]

##[<] 🤖🤖