# [>] 🤖
context:
  function: cli.loader.Root
cases:
  - name: ShouldWireEverySubcommandToRoot
    expected:
      output:
        - make-links
        - make-copies
        - render-templates
        - make-dirs
        - prune-links
        - run-scripts
        - discover
# [<] 🤖
