# [>] 🤖🤖
context:
  function: lib.tagComment
cases:
  - name: ShouldParseValsLabel
    input:
      args: [line: "#[vals] delta|all"]
    expected:
      output: {vals: delta|all, ok: true}
  - name: ShouldRejectWhatLabel
    input:
      args: [line: "#[what] not vals"]
    expected:
      output: {vals: "", ok: false}
  - name: ShouldRejectWhyLabel
    input:
      args: [line: "#[why] nope"]
    expected:
      output: {vals: "", ok: false}
# [<] 🤖🤖
