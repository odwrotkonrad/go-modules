# che.yml Reference

Reserved top-level keys: `options`, `env`, `variables`, `include`. Every other
key is a profile block. Schema:
[che.schema.json](../assets/data/che.schema.json), draft 2020-12, generated by
`$ go run ./internal/docgen`, drift-checked pre-commit.

## Files

Two kinds of spec, told apart by name:

- `che.export.yml`: a spec designed for reuse by other repos. At the repo
  root, or at the root of each config when a repo holds several distinct ones
  (`profiles/claude/che.export.yml`).
- `che.yml`: the repo's own config, under `.che/` (`.che/che.yml`), or in a
  subdirectory of `.che/` when a repo holds several (`.che/ci/che.yml`). A
  root `che.yml` is still found, `.che/che.yml` is the layout to use.

A consumer never names a spec file in a URL: it names the repo or dir
(`source`), at most a dir or file under it (`spec`), and a `profile`. The
search finds `che.export.yml` first; a plain `che.yml` still resolves, not
recommended: che warns, the author of a `che.yml` never promised it stays.

Three files per repo, each looked up at the repo root first, then under
`.che/`. A root file wins over its `.che/` twin (debug log). Layout:

```
che.export.yml                  # reusable profiles, if this repo exports any; host configs live here or at root che.yml
.che/
  che.yml                       # the repo's own spec: composes the two below, holds what fits neither
  che.variables.yml             # repo variables, refs first of all, flat KEY: value
  che.env                       # repo env, dotenv syntax, beneath the process env
  repo-git-tracked/
    che.yml                     # profiles generating tracked files (README, LICENSE, purpose docs)
    templates/                  # this repo's own templates for them
  repo-git-untracked/
    che.yml                     # profiles generating gitignored files (.env, AGENTS.md, .claude/, shared CI)
    templates/
```

`.che/che.yml` composes the two sub-specs with `include.sources:
[.che/repo-git-tracked, .che/repo-git-untracked]`, each present only when
the repo generates files of that kind. The two never refer to each other: a
profile one of them needs from the other runs on its own (`autoDiscover`, or
named by `--profiles`), the composing `.che/che.yml` is the only place that
sees both. A repo defining its own templates keeps
them under `.che/<kind>/templates/`. No `.che/host`: host configuration is
private to a host, so it belongs in a root `che.yml` or `che.export.yml`, or
in a directory whose purpose is one host or a set of hosts, never under the
repo-scoped `.che/`.

Sources in a sub-spec resolve against the sub-spec's dir (`templates/x.tpl`),
scripts and `profileWorkingDirectory` against the spec's checkout; a
repo-relative dest, an `@`-include and a `localFile` path resolve against the
git root of the spec, so a sub-spec under `.che/` renders onto the repo.
`--profiles` names a profile of the invoked spec or of any local spec it
composes.
Outside a git repo the root is the nearest ancestor holding `che.yml` or
`.che/che.yml`. The same lookup applies to every included spec
(`include.sources` dirs, `git::` clones, `//<subdir>` paths). `.env` is not
read.

## Editor Validation

First line of `che.yml`:

```yaml
# yaml-language-server: $schema=https://gitlab.com/api/v4/projects/konradodwrot%2Fgo-modules/packages/generic/che-schema/v0.0.1-che-min-v0.0.96/che.schema.json
```

Pin `main` to the tag of the installed che. Each tag snapshots the schema,
also shipped as a release asset.

## Load Validation

Every loaded che.yml validates against this schema: local repo, composed
specs, sourced-ref checkouts. Violations warn. `--validate-spec error` (env
`CHE_VALIDATE_SPEC=error`) aborts.

## Full Example

Every construct, schema-validated by the test suite:

```yaml
# yaml-language-server: $schema=https://gitlab.com/api/v4/projects/konradodwrot%2Fgo-modules/packages/generic/che-schema/v0.0.1-che-min-v0.0.96/che.schema.json
options:
  profileWorkingDirectory: root
  runIf: ['env:CHE_ENABLED']
  autoDiscover: false
  logLevel: info
  validateSpec: warn
env:
  SOME_VAR: value
variables:
  SHARED_REF: v1.2.0
include:
  sources:
    - ./sibling-spec
    - "git::git@gitlab.com:group/specs.git"
    - source: "git::git@gitlab.com:group/shared.git@${{ var.SHARED_REF }}"
      spec: profiles/shared
      env: {SOME_VAR: other}
      variables: {repo: Example}
base:
  include:
    makeLinks:
      - {source: _home/**, dest: $HOME/**}
      - etc/{grafana,prometheus}/**
    makeCopies:
      - {source: _home/**, dest: $HOME/**}
      - owner: root
        ownerGroup: "0"
        chmod: "0644"
        source: Library/LaunchDaemons/otelcol.plist.ontoHost.cp
        dest: [/Library/LaunchDaemons/otelcol.plist]
      - source: "git::gitlab.com/org/prompts@v1.2.0"
        dest: /usr/local/share/prompts
        <<<:
          - {source: //commit.md.tpl, dest: commit.md.tpl}
          - {source: //mr.md.tpl, dest: mr.md.tpl}
    renderTemplates:
      - {source: _home/**, dest: $HOME/**}
      - owner: root
        ownerGroup: "0"
        chmod: "0440"
        source: etc/sudoers.d/configs.ontoHost.tpl
        dest: [/etc/sudoers.d/configs]
    makeDirs:
      - directories:
          - $HOME/.local/{bin,share}
      - chmod: "2775"
        directories:
          - {dest: ["/var/log/{grafana,prometheus}"]}
    runScripts:
      - ci/zsh/scripts/installs/*.zsh

base-exclude-cli:
  exclude:
    makeLinks:
      - etc/{grafana,prometheus}/**
    makeCopies:
      - Library/LaunchDaemons/otelcol.plist*
    renderTemplates:
      - etc/sudoers.d/**
    makeDirs:
      - /var/log/{grafana,prometheus}
    runScripts:
      - ci/zsh/scripts/installs/80-desktop-tools.zsh

desktop/macos:
  options: {autoDiscover: true, runIf: ['builtin:isOs == macos', 'builtin:isVirt == false']}
  include:
    profiles:
      - base
      - source: "git::git@gitlab.com:group/repo.git"
        profile: someProfile
      - source: "git::https://gitlab.com/group/other.git"
        profile: otherProfile
        env:
          SOME_VAR: value
        variables:
          repo: Example

cli/linux:
  options: {autoDiscover: true, runIf: ['builtin:isOs == linux']}
  include:
    profiles: [base, base-exclude-cli]

ontoRepo:
  options: {autoDiscover: true}
  include:
    renderTemplates:
      - source: templates/local.env.ontoRepo.tpl
        dest:
          - {path: .env, options: {writeType: mergeUpsert}}
      - source: templates/AGENTS.md.ontoRepo.tpl
        dest:
          - CLAUDE.md
          - {path: AGENTS.md, options: {renderReferencedFiles: true}}
      - source: templates/Makefile.docs.tpl
        dest:
          - {path: Makefile, options: {writeType: partial, commentPrefix: "#"}}
```

## Reserved Top-Level Keys

### options

Spec-wide defaults and che knobs:

- `runIf` (string list): gates every profile of this spec (profile runIf
  grammar).
- `autoDiscover` (bool), `logLevel` (`error`|`warn`|`info`|`debug`|`trace`):
  defaults for profiles that don't set them.
- `profileWorkingDirectory` (path): load-ops source tree, default `.` (the checkout).
  Absolute, `~/`, `$VAR` expand. Relative resolves under the checkout.
  makeLinks/makeCopies globs and renderTemplates host sources resolve against
  it. Home targeting is an explicit `$HOME` dest rewrite, no implicit `_home/`
  mapping. che.yml lookup, scripts, and repo-doc template sources (repo dest)
  stay at the checkout.
- `validateSpec` (`warn` | `error`): top-level only, flag and env override.
- `envUnset` (`error` | `empty`): what a bare `${{ env.NAME }}` does when
  `NAME` is unset or empty, see [Interpolation](#interpolation). Top-level
  only, literal only (read before interpolation), `--env-unset` and
  `CHE_ENV_UNSET` override. Default `error`.
- `dryRun` (`delta` | `all` | `true` | `false`): default dry-run mode (`true`
  aliases `delta`, `false` the default), flag and env override.
- `profiles` (string list): profiles to run (autoDiscover skipped, runIf still
  enforced), `--profiles` and `CHE_PROFILE` override.
- `skipRemoteRefs` (bool): skip sourced `include.profiles` refs, flag and env
  override.
- `renderTemplates.skipVariables` (bool): skip templates carrying a `shell`
  call, flag and env override.
<!-- [>] 🤖 -->
- `skipOps` (op-name list): ops skipped everywhere: dropped from the run
  sequence, direct op subcommands become logged no-ops. `--skip-ops` and
  `CHE_SKIP_OPS` override.
- `run.skipOps` (op-name list): ops skipped in the run sequence only (direct op
  subcommands unaffected). `che run --skip-ops` and `CHE_RUN_SKIP_OPS` override.
- `packages`: installPackages defaults: `file` (packages.yml path),
  `preferredInstallationMethods`,
  `binariesRemoteArchive.installDestinationCandidates`,
  `completions.zsh.installDestinationCandidates`,
  `manpages.installDestinationCandidates`, `source` (`url`, `ref`),
  `autoUpdate` (`enabled`, `if.refIsLatest.cooldown`,
  `if.dryRunIsTrue.enabled`). Cascades profile > spec > user config.
- `otel`: OTLP telemetry knobs (`enabled`, `endpoint`, `protocol`, `metrics`,
  `logs`, `traces`), `CHE_OTEL_*` env wins. See
  [observability.md](observability.md).
<!-- [<] 🤖 -->

Cascade, most nested wins: profile options > spec top-level options > che level
(flags, env vars, user-config file, local spec `options:`) > defaults. See
[Environment](#environment).

The user-config file (`$XDG_CONFIG_HOME/che/config.yml`) takes this same shape
as a bare object, without the `options:` wrapper. `runIf` and
`profileWorkingDirectory` are spec-only, ignored there.

### env

`KEY: value` map exported around this spec's preparation and execution. Values
may interpolate `${{ env.NAME }}` from the surrounding env layers (never
from sibling keys) and `${{ var.NAME }}`. The block then feeds the rest of
the file's interpolation. Where it sits in the lookup:

- Root spec: repo `che.env` < process env < this `env:` < a ref's `env:`.
- Included spec (`include.sources` entry or `include.profiles` ref): its own
  repo's `che.env` < its `env:` < the invoking spec's whole effective env <
  the entry's `env:`. Env inherits through every include level: what the
  invoker sees, the included spec sees, and the invoker wins.

### variables

`KEY: value` map, `${{ var.NAME }}` in this spec and `.var.NAME` in its
templates. Values may interpolate `${{ env.NAME }}`, never sibling
variables. Keys match `[A-Za-z_][A-Za-z0-9_]*`. Lookup, most specific last:

```
effective env NAME  <  che.variables.yml  <  spec variables:  <  ref variables:
(implicit)             (explicit, repo)      (explicit, spec)    (explicit, per ref)
```

Refs live in `che.variables.yml`. Variables never inherit across an include:
an included spec resolves its own repo's `che.variables.yml` and its own
`variables:`, so the included repo controls what it includes. The invoking
side sets a variable in that scope only on purpose, per need, through the
ref's `variables:` (an `include.sources` entry or an `include.profiles` ref),
which wins over everything:

```yaml
- source: 'git::gitlab.com/org/ai-tools-configs@${{ var.AI_TOOLS_CONFIGS_REF }}'
  profile: claude/virt
  variables:
    repo: Notes
```

The effective env ([env layering](#env): `che.env`, a shell export, a CI job
variable, an inherited env) sets a variable implicitly, effective only while
no file declares that name. One name lives in exactly one of `che.env` and
`che.variables.yml`.

### include

`sources`: other specs composed into this one, as if run together. Each entry
is a scalar `<dir>` (absolute, relative, `~/`, `$VAR`) or
`git::<giturl>[@<ref>]` (cloned/pulled into a managed cache checkout), or a
map `{source, spec, env, variables}`: `spec` as on a profile ref (a dir
searched `che.export.yml` first, or a file), `env` and `variables` overlay
every profile of the included spec. A composed spec keeps its own anchor, files and
profile eligibility. Its profile names become referenceable from this spec's
`include.profiles` (bare-name collisions error). Duplicates and cycles load
once. Recursive.

## Interpolation

Any string value in any che.yml (local, included, sourced) may carry
`${{ env.NAME }}`, `${{ var.NAME }}` or either with `|| default`.
Mapping keys (profile names) stay literal. Substituted values stay strings.

```yaml
variables:
  SHARED_REF: ${{ env.SHARED_REF_OVERRIDE || v1.2.0 }}
env:
  TREE: ${{ env.BASE_TREE || root }}
web:
  options: {profileWorkingDirectory: '${{ env.TREE }}'}
  include:
    profiles:
      - source: 'git::gitlab.com/org/shared@${{ var.SHARED_REF }}'
        profile: app
        env: {APP_NAME: web}
        variables: {repo: Web}
```

- `NAME` matches `[A-Za-z_][A-Za-z0-9_]*`, whitespace inside `${{ }}` is
  insignificant. Anything short of the full form passes through untouched,
  `$VAR` dest expansion included.
- The default is literal: everything after the first `||` up to `}}`, trimmed,
  never itself interpolated. A defaulted ref never errors.
- Unset means absent or empty.
- `env.` lookup: see [env](#env). `var.` lookup: see [variables](#variables).
- A bare `env.` ref on an unset var follows `options.envUnset`. Under `error`
  (the default) the load fails only for profiles selected to run
  (`--profiles`, auto-discovery, sourced refs): a profile skipped by selection
  or `runIf` never demands its vars. Top-level keys (`env:`, `options:`,
  `include:`) are always strict. All unset refs of a spec report in one error,
  each with its YAML path. Under `empty` the ref reads as `""`.
- A bare `var.` ref on an undeclared variable always fails the load, naming
  the spec file and every gap with its YAML path.
- `che discover-profiles` never fails on unset env refs. It lists every ref
  per spec, its default if any, whether it is set and from which source, and
  skips profiles with unset refs.

### che.env

`che.env` (root or `.che/`, dotenv syntax, `export` prefix tolerated, missing
file fine) is the repo's env, beneath the process env: a shell export wins
over the file. Every spec under that git root reads it, remote clones read
their own. Values interpolate `${{ env.NAME }}` from the process env,
never `${{ var.NAME }}`. A name set here is env for scripts and
templates, and an implicit variable while no `che.variables.yml` or
`variables:` declares it. Refs belong in `che.variables.yml`.

```sh
# .che/che.env
PROSE_ASSETS_REF=v0.0.67
```

### che.variables.yml

`che.variables.yml` (root or `.che/`) is a flat `KEY: value` map, the repo's
explicit variables beneath every spec's `variables:` block. Values may
interpolate `${{ env.NAME }}` from the process env, never
`${{ var.NAME }}`. Every spec under that git root reads it, remote clones
read their own. The preferred home of a repo's refs. A name declared here
wins over env, an invoking repo's included; only a ref's `variables:` sets it
from outside.

```yaml
PROSE_ASSETS_REF: v0.0.67
MISC_REF: v0.0.38
```

## Profile Block

```yaml
<profile-name>:
  options: {autoDiscover: true, runIf: ['builtin:isOs == macos']}
  include: {...}
  exclude: {...}
```

Names are free-form (`base`, `cli/macos`, `ontoRepo`). Bare `$ che` runs every
eligible profile's full op sequence, profile by profile.
`$ che --profiles <name>[,<name>...]` runs only those.

### options

- `autoDiscover` (bool): run on bare `$ che`. Unset: inherit spec
  `options.autoDiscover`, then `false` (runs only via `--profiles` or
  `include.profiles`).
- `runIf` (string list): predicates, all must pass. `<source>` (truthy:
  builtin iff `true`, env iff set non-empty, cmd iff exit 0) or
  `<source> == <literal>` (string compare). Sources: `builtin:isOs`
  (`macos`/`linux`), `builtin:isVirt` (`true`/`false`), `env:<NAME>`,
  `cmd:<argv>` (`true`/`false`). Empty: always. `--skip-run-if` (env
  `CHE_SKIP_RUN_IF`): all pass.
  `cmd:<argv>` passes on exit 0. Gates on state no variable holds (a tool
  authenticated, a daemon answering). stdout and stderr are discarded: the
  exit code is the whole contract. Evaluated on every use, never cached like
  the immutable `builtin:` sources. The argv splits on whitespace and executes
  directly, no shell: quotes and operators are not interpreted, a command
  needing shell syntax names its interpreter (`cmd:zsh -c ...` passes each
  whitespace-separated word as its own argument).
- `logLevel` (`error`|`warn`|`info`|`debug`|`trace`): human-log level around
  this profile's ops. Unset: inherit spec, then che level.
- `profileWorkingDirectory` (path): the profile's load-ops source tree. Unset: inherit
  spec, then che level (`--working-directory` / `CHE_WORKING_DIRECTORY`), then
  `.`.
- `packages`: installPackages options for this profile (same shape as spec
  `options.packages`). Empty fields inherit spec options, then user config.

## Dest Mapping

A working-tree path maps onto a system-root path (`etc/x` -> `/etc/x`), no
implicit `_home/` mapping. A dest resolving absolute (`/...`) or expanding
`$HOME` lands there directly. By convention host-home files sit under `_home/`
(leading underscore, so it can't collide with a real `home` dir), rewritten
onto `$HOME`.

Dest strings expand env vars, `$HOME` bound to the invoking user's home, so
sudo (different process `$HOME`) stays correct. Home targeting per file, as a
glob prefix-swap, or as a glob sed rewrite:

```yaml
makeLinks:
  - {source: _home/**, dest: $HOME/**}              # prefix-swap sugar
makeCopies:
  - {source: _home/**, dest: 's:^_home:$HOME:'}  # glob + sed rewrite
  - {source: _home/foo.ontoHost.cp, dest: [$HOME/.config/foo]}  # explicit
renderTemplates:
  - {source: _home/**, dest: $HOME/**}
  - {source: _home/x.tpl, dest: [$HOME/.config/x]}
makeDirs:
  - directories: [$HOME/.local/{bin,share}]
```

The prefix-swap sugar `{source: <src>/**, dest: <dst>/**}` desugars to
`s:^<src>:<dst>:`, stripping both `/**`. The sed rewrite
`s<delim><pattern><delim><replacement><delim>[g]` (any `<delim>` after `s`,
`:` the blessed form, Go regexp pattern, literal replacement so `$HOME`
survives to host mapping) rewrites the profileWorkingDirectory-relative dest
before host mapping.

## Environment

### Env layers

One effective env per spec, most specific last:

| Layer | Root spec | Included spec |
| --- | --- | --- |
| 1 | repo `che.env` | its repo's `che.env` |
| 2 | process env | its `env:` |
| 3 | its `env:` | the invoking spec's effective env |
| 4 | a ref's `env:` | the entry's `env:` |

`che discover-profiles` names the layer each `${{ env.NAME }}` resolved from.

Variables, most specific last: the effective env (implicit) <
`che.variables.yml` < spec `variables:` < the ref's `variables:`. `.var.NAME` in a
template and `${{ var.NAME }}` in a spec read the same chain.

### Runtime options

Every flag has a `CHE_*` env mirror, flag wins. Full list: Global options
table in `cli.md`.

Per-field resolution, most specific wins:

```
flag > CHE_* env > user-config file > local che.yml options: > default
```

### User-config file

`$XDG_CONFIG_HOME/che/config.yml` (see Paths) sets runtime options for every che
run on this machine: over the repo's `che.yml` `options:`, under env vars and
flags. A bare object in the che.yml [`options:`](#options) shape, without the
`options:` wrapper:

```yaml
# ~/.config/che/config.yml
logLevel: debug
validateSpec: error
dryRun: delta
autoDiscover: true
profiles: [cli/macos, work]
skipRemoteRefs: true
renderTemplates:
  skipVariables: true
```

`autoDiscover: true` here discovers every profile that sets neither its own nor
its spec's `autoDiscover`. A profile's `autoDiscover: false` still wins. `runIf`
and `profileWorkingDirectory` are spec-only, ignored here.

### Paths (XDG)

Runtime data follows the XDG base-dir spec, each base overridable by a che env:

| Purpose | Path under base | XDG base (`+/che`) | che override | Fallback |
| --- | --- | --- | --- | --- |
| user-config file | `config.yml` | `XDG_CONFIG_HOME` | `CHE_CONFIG_HOME` | `~/.config/che` |
| remote source caches | `remote-sources/<slug>` | `XDG_CACHE_HOME` | `CHE_CACHE_HOME` | `~/.cache/che` |
| backup archives | `backups/<profile-slug>/<op>/<ts>-<backup-id>.tar.bz2` | `XDG_STATE_HOME` | `CHE_STATE_HOME` | `~/.local/state/che` |

Precedence per base, most specific wins: `CHE_<X>_HOME` > `XDG_<X>_HOME/che` >
fallback. `CHE_*` is che's base as-is, the XDG base gets `/che` appended.
`CHE_CACHE_HOME=/tmp/c` -> `/tmp/c/remote-sources`, `XDG_CACHE_HOME=/x` ->
`/x/che/remote-sources`, neither -> `~/.cache/che/remote-sources`.

## include

Additive. Eight sections:

<!-- [>] 🤖 -->
`profiles`, `makeLinks`, `makeCopies`, `renderTemplates`, `makeDirs`,
`installPackages`, `installToolPackages`, `runScripts`.
<!-- [<] 🤖 -->

### profiles

Profile refs composed depth-first, in order, before this profile's own payload.
A scalar names a local profile (cycles and undefined names error). A map
references a profile in another spec, loaded and anchored at its own checkout.

```yaml
include:
  profiles:
    - base
    - source: "git::https://gitlab.com/konradodwrot/ai-harness/ai-tools-configs@${{ var.AI_TOOLS_CONFIGS_REF }}"
      profile: claude/virt
      variables: {repo: Notes}
    - source: "git::https://gitlab.com/konradodwrot/tools-configs@${{ var.TOOLS_CONFIGS_REF }}"
      profiles:
        - base/packages
        - cli/macos::shell/host/macos
        - {spec: profiles/dev, profile: dev/host/macos}
    - source: ./profiles/git
      profile: git/host/macos
```

- `source`: where the spec lives. `git::<giturl>[@<ref>]` (remote, cloned/pulled
  into a managed cache checkout) or `<dir>` (local, used in place, no git:
  `$VAR` and `~` expand, a relative path resolves against the referencing
  spec's checkout, `.` is this repo's own exported spec). Never a file, never
  `//<path>`: both are rejected at load, naming the keys to use.
- `@<git-ref>` pins a remote source to a tag or branch, its own cache
  checkout per ref, unset means the default branch HEAD. Anchored to the last
  `@` after the URL, so an SCP user (`git@host:group/repo.git`) is never
  mistaken for it. Remote only: a local dir keeps any `@` as a literal name.
  The same suffix pins `include.sources` entries. `?ref=` is rejected.
- `spec`: where the spec sits under `source`, default the source root. A
  dir: the standard search, `che.export.yml` at the dir, then
  `.che/che.export.yml` (warned: hidden dir), else a plain `che.yml` at the
  dir or `.che/che.yml` (warned on a remote source: not designed for reuse;
  two plain ones and no export: error). Two `che.export.yml` present: both
  load, the profile is searched root-first. A spec file path (any `.yml`) is
  taken as is, profiles resolved relative to that file. Not recommended: the
  top-level `che.export.yml` with a profile path is the way to a nested
  profile.
- `profile`: a top-level profile of that spec, or a path `a::b::c`: `a` is
  top-level, `b` a profile `a` includes (a local name or a sourced entry,
  the walk follows it into its own source), `c` one `b` includes. Nested
  profiles are never searched by default: `profile: c` alone does not find
  it. A profile no spec defines is not an error: che warns at load, skips
  the ref, and repeats every unresolved ref after the run.
- `profiles`: several profiles of one source and ref, the URL and the pin
  written once, the entry's `spec` shared by every item so a spec location is
  written once too. Items are profile names or `a::b` paths, or
  `{spec, profile}` for a profile under another spec of the same source.
  `options`, `env` and `variables` apply to each. Exclusive with `profile`.
- `options`: override the referenced profile's own. One more cascade level, most
  nested wins.
- `env`: exported around everything done for the referenced profile,
  sourced entries only. The referenced spec's `env:` merges under it, entry
  wins, at load too: the referenced spec's own `${{ env.NAME }}` refs see
  the entry's values. See [env](#env) for the full layering.
- `variables`: override the referenced spec's variables (its repo's
  `che.variables.yml` and its `variables:` block), sourced entries only. In
  templates they also win over every node's `variables`. Parameterizes a
  shared profile per consumer: `variables: {repo: Notes}`.

Sourced refs resolve recursively, their own `include.sources` and sourced refs
included, deduped by source plus profile, cycle-guarded. `--skip-remote-refs`
(env `CHE_SKIP_REMOTE_REFS`) drops all sourced refs.

### makeLinks

Entries over git-tracked files under the profileWorkingDirectory, relative to it.
Outside a git repo the spec anchors at the nearest ancestor dir with a `che.yml`
and file selection falls back to a filesystem walk. Symlinks point at the
derived host path (`etc/x` -> `/etc/x`, home via a `$HOME` dest rewrite).
Templates, `*.ontoHost.cp` and `.gitkeep` never link.

Items: glob string (brace-expanded, dest derived 1:1),
`{source, dest: [paths]}` (one source file, explicit dests, `~/` or absolute ->
host), or `{source, dest: <rule>}` where `source` is a file or glob and `dest`
a sed-style rewrite `s<delim><pattern><delim><replacement><delim>[g]` (any
`<delim>` after `s`, Go regexp pattern, literal replacement, `\<delim>` escapes
a literal delimiter, `g` rewrites every match, absent means first only) applied
to the repo-relative dest before host mapping.

```yaml
include:
  makeLinks:
    - {source: _home/**, dest: $HOME/**}
    - etc/{grafana,prometheus}/**
    - {source: _home/.config/foo/**, dest: 's:^_home/.config/foo:$HOME/.config/bar:'}
    - {source: files/config, dest: [$HOME/.config/mypy/config]}
```

First entry: `_home/.config/x` -> `~/.config/x`. Rewrite entry:
`_home/.config/foo/x` -> `~/.config/bar/x`. Explicit-dest entry links one
source file to each listed host path.

### makeCopies

A tree of sources copied onto the host byte-for-byte, never rendered: a
gomplate-bearing file lands intact. Same node shape as renderTemplates, minus
`variables` and `options`: a leaf (glob string, or `{source, dest}`) or a group (a
node with nested `<<<`, its `source` and `dest` prefixes and perms cascading
onto every descendant, innermost wins).

Local sources are `*.ontoHost.cp` files, profileWorkingDirectory-relative. A
source may be remote, `git::<repo>[@<ref>]//<path>`, explicit dest required; a
group prefix carrying the ref concatenates with each leaf path so the pin is
typed once. Remote globs and remote dest rewrites are rejected at load.

```yaml
makeCopies:
  - {source: _home/**, dest: 's:^_home:$HOME:'}
  - owner: root
    ownerGroup: "0"
    chmod: "0644"
    source: Library/LaunchDaemons/otelcol.plist.ontoHost.cp
    dest: [/Library/LaunchDaemons/otelcol.plist]
  - source: "git::gitlab.com/org/prompts@v1.2.0"
    dest: /usr/local/share/prompts
    chmod: "0644"
    <<<:
      - {source: //commit.md.tpl, dest: commit.md.tpl}
      - {source: //mr.md.tpl, dest: mr.md.tpl}
```

Leaves: glob string (derived dest, `.ontoHost.cp` stripped),
`{source, dest: [paths]}` (one source, explicit host dests), or
`{source, dest: <rule>}` (glob source + sed-style dest rewrite, `.ontoHost.cp`
stripped first). Perms sit on the leaf itself, or on a group when several
leaves share them. A group `dest` is one path, prefixed onto each nested
relative dest; `~/`, absolute and `$VAR` dests anchor themselves.

### renderTemplates

A tree of `*.tpl` sources rendered through gomplate. The template root is
`{env, var}`: `.env.NAME` reads the profile's effective env (repo `che.env`,
process, spec `env:`, inherited and ref `env:`), `.var.NAME` its variables
(effective env < repo `che.variables.yml` < spec `variables:` < the node's
`variables` < the ref's `variables:`). A missing key fails the render. The same env is exported
around each profile's ops, so `env.Getenv "NAME"` reads it too.
`{{ shell "<command>" }}` runs the command with `-c` under the user's shell
(`$SHELL`, else the login shell, else `sh`), in the repo root with the process
env, and substitutes its stdout, trailing newline trimmed. Non-zero exit fails
the render, naming the template, command and stderr. Dest path decides the
target: relative -> repo, `~/` or absolute -> host. Host sources (glob form,
derived dest, or any `~/`/absolute dest) and repo-doc sources (repo dest) are
both profileWorkingDirectory-relative. Glob and dest-omitted forms derive a
host dest.

A source may be remote, `git::<repo>[@<ref>]//<path>`, explicit dest required.
Dests expand env vars, including `${invokingSpecGitRoot}`, the top-level
spec's checkout.

Each item is a node: a leaf (glob string, or `{source, dest}`) or a group (a
node with its own nested `renderTemplates`).

```yaml
renderTemplates:
  - {source: _home/**, dest: 's:^_home:$HOME:'}
  - source: templates/1-env/local.env.ontoRepo.tpl
    dest:
      - {path: .env, options: {writeType: mergeUpsert}}
  - source: templates/3-audience/AGENTS.md.ontoRepo.tpl
    dest:
      - CLAUDE.md
      - {path: AGENTS.md, options: {renderReferencedFiles: true}}
```

A glob source may also carry `{source, dest: <rule>}`: sed-style dest rewrite,
template suffix stripped first, raw-body host dest like the bare glob form.

Dest is a path string, list of paths, or `{path, options}`:

- `writeType`: `""` (overwrite, the default, autogen header plus body) |
  `mergeUpsert` (env `KEY=VALUE` union under existing dest, no header) |
  `partial` (section injection, no header). Under `mergeUpsert` a `shell`
  value overwrites its existing key and a plain value keeps it;
  `| alwaysUpdate` and `| keepIfExisting` after any expression decide per
  line, and piping a whole multi-line block through one marks every
  `KEY=VALUE` line in it, so `{{ localFile ".repo/upstream.env" |
  alwaysUpdate }}` pulls a whole lockfile in under one marker.
- `commentPrefix` (string): the line-comment prefix section markers sit
  behind (`#`, `//`, `--`, `<!--`). Required with `partial`, rejected with any
  other `writeType`.
- `skipAutoGeneratedHeader` (bool): drop the autogen header on overwrite, for
  formats that forbid comments, e.g. private keys. The header names the
  template source only, never a ref.
- `renderReferencedFiles` (bool): inline `@`-includes, overwrite path.

#### Partial render

`writeType: partial` renders sections into a hand-maintained file. The
rendered body carries one or more sections, each a `[>] <name>` line and a
`[<] <name>` line behind `commentPrefix`; the dest carries sections of the
same shape. Each top-level rendered section replaces the body of the
same-named dest section, markers kept, everything outside untouched. Sections
nest: a rendered outer section replaces its dest twin whole, inner included; a
rendered inner-only section replaces just that inner one. A rendered section
the dest lacks fails the render, naming the section, the template and the
dest, and the dest is left as it was. A missing dest takes the rendered body
whole. Rendering twice yields the same file.

```yaml
renderTemplates:
  - source: templates/Makefile.docs.tpl
    dest:
      - {path: Makefile, options: {writeType: partial, commentPrefix: "#"}}
```

```makefile
hand-written: ...

##[>] docs
render-docs:
	che render makefile-doc Makefile
##[<] docs
```

A scalar `dest` is a rewrite rule only when it looks like one (`s<delim>…` or
`<prefix>/**`), otherwise a plain path.

#### Groups

A group node sets `source` and `dest` prefixes, `variables`, `options` and
perms for everything nested under it, at any depth.

```yaml
renderTemplates:
  - source: "git::gitlab.com/konradodwrot/cross-repo/prose/assets@v0.0.38"
    options: {skipAutoGeneratedHeader: true}
    <<<:
      - {source: //repos/configs/purpose.md, dest: assets/docs-agents/purpose.md}
      - source: //repos/configs/ai/claude-rules
        dest: root/_home/.config/claude/rules
        <<<:
          - {source: /code/code.md, dest: code/code.md}
          - {source: /docs/prose.md, dest: docs/prose.md}
```

Cascade, outermost first, innermost wins:

- `source`: joined as a prefix. The ref rides the repo, so a remote prefix and
  its leaves plainly concatenate: `git::<repo>@<ref>` plus leaf `//<path>`
  yields `git::<repo>@<ref>//<path>`. One ref per group: a leaf is a path and
  carries none, so pinning a leaf apart means its own top-level entry. A local
  prefix joins as a path.
- `dest`: one path on a group, joined onto every descendant's repo-relative
  dest. Host dests anchor themselves: a descendant dest starting with `/`, `~`
  or `$` is left alone. A group dest takes no per-dest `options`.
- `owner`, `ownerGroup`, `chmod`: per field.
- `variables`: per key, exposed as `.var.<key>`.
- `options`: per field, each dest's own `options` winning last. An explicit
  `false` overrides an inherited `true`: a group carries the common case, a
  leaf opts out.

A group assembles paths: it carries a `source` prefix, a `dest` prefix, or
both. A node with nested `<<<` and neither is rejected at load, as is one
carrying a dest rewrite rule or a glob. Perms, `variables` or `options` shared
by leaves that share no path go on each leaf.

makeCopies groups follow the same rules, minus `variables` and `options`.

### makeDirs

Perm-groups of dirs to create, beyond the ancestors derived from links, copies
and derived-dest templates. Items: path string (brace-expanded), or `{dest:
[paths]}`.

```yaml
makeDirs:
  - directories:
      - $HOME/.local/{bin,share}
  - chmod: "2775"
    directories:
      - {dest: ["/var/log/{grafana,prometheus}"]}
```

### installPackages

<!-- [>] 🤖 -->
Packages installed from the packages file, default
`$XDG_CONFIG_HOME/packages/packages.yml`, relocated by `--packages-file`,
`packages.file` or `CHE_PACKAGES_FILE`. Runs after renderTemplates, before
runScripts. Items: canonical name scalar, or `{name, version}` pinning over the
entry's default. Composed profiles' lists concatenate and dedupe.
`exclude.installPackages` drops matching names.

```yaml
include:
  installPackages:
    - ripgrep
    - {name: terraform, version: "1.10"}
```
<!-- [<] 🤖 -->

### installToolPackages

<!-- [>] 🤖 -->
Tool-scoped packages from the packages file's `toolPackages` section, keyed by
host tool. Runs with installPackages. Items per tool: name scalar, or
`{name, version}` pinning over the entry's value. Composed profiles' lists
concatenate per tool, later refs re-pin by name. `exclude.installToolPackages`
drops names per tool.

```yaml
include:
  installToolPackages:
    vscode:
      - golang.go
      - {name: ms-python.python, version: "2024.14.0"}
```
<!-- [<] 🤖 -->

### runScripts

Script paths or globs, repo-relative, run in spec order. Globs expand sorted
in place. Every entry must match >=1 file.

```yaml
runScripts:
  - ci/zsh/scripts/installs/*.zsh
```

## exclude

Subtractive glob filter, applied last, winning over every include, rich entries
too. Seven keys mirror include's op sections (`profiles` has none): six flat
string lists, plus `installToolPackages`, a per-tool map of string lists. A
match on source or any dest drops the item. installPackages matches canonical
names.

```yaml
exclude:
  makeLinks: [_home/Library/**]
  makeCopies: [Library/LaunchDaemons/grafana.plist*]
  renderTemplates: [_home/.gitlab-runner/**]
  makeDirs: [/var/log/grafana]
  installPackages: [terraform]
  installToolPackages:
    vscode: [ms-python.python]
  runScripts: [ci/zsh/scripts/installs/80-go-host-tools.zsh]
```

## Perms Cascade

`owner`, `ownerGroup`, `chmod` on a makeDirs group apply to every item in its
`directories`, on a makeCopies/renderTemplates group to every nested node at
any depth. Innermost non-empty field wins. Glob matches count too, last
matching glob wins. Empty falls back to the code default.

## See Also

- [cli.md](cli.md): generated CLI reference.
- [che.schema.json](../assets/data/che.schema.json): generated JSON Schema.
