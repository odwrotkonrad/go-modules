# [>] 🤖🤖
context:
  function: testyml.Eq
cases:
  - name: ShouldAssertTypedOutput
    input:
      args: [s: abc]
    expected:
      output: ABC
  - name: ShouldApplyContextEnv
    context:
      env: {TESTYML_EQ_ENV: from-env}
    input:
      args: [s: $TESTYML_EQ_ENV]
    expected:
      output: FROM-ENV
  - name: ShouldStopAtWantedError
    input:
      args: [s: abc, fail: true]
    expected:
      errorOutput: [stub fail]
# [<] 🤖🤖
